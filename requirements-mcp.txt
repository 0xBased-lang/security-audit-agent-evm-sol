# MCP Server Dependencies
# Install with: pip install -r requirements-mcp.txt

# MCP SDK
mcp>=0.9.0

# Security Tools (optional, install separately if needed)
# slither-analyzer  # Install separately: pip install slither-analyzer
# mythril  # Install separately: pip install mythril

# Foundry is installed via: curl -L https://foundry.paradigm.xyz | bash
# Echidna: https://github.com/crytic/echidna

# Python standard library (no additional deps required)
